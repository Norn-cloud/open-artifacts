scopes:
    - name: skills
      description: core logic for skill execution and management in skills/; does not cover framework setup or tests
    - name: src
      description: primary application business logic and services in src/; does not cover skill-specific implementation or scripts
    - name: scripts
      description: utility scripts for vendor management, bundling, and build tasks in scripts/; does not cover application logic
    - name: tests
      description: test suites, feature definitions, and integration harnesses in tests/; does not cover source code or build scripts
